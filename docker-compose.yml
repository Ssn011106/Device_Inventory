version: '3.8'

services:
  devicetracker:
    build: .
    container_name: devicetracker-app
    restart: always
    ports:
      - "3000:3000"
    environment:
      - MONGODB_URI=${MONGODB_URI:-mongodb://10.225.205.127:27017/devicetracker}
      - PORT=3000
